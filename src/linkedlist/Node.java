package linkedlist;

public class Node {
 
    int data;
    Node next;

    // Constructor to initialize the node with data
    //next will contains details of the next node
    public Node(int data)
    {
        this.data = data;
        this.next = null;
    }
}
